# Entscheidungslog

## 2026-08-13/14 – Grundsatzentscheidungen (Planungsphase)

1. **Plattform: TPB Pure** – strukturierter framework-freier PHP/MySQL-Eigenbau statt WordPress/WooCommerce (Begründung: Systemkonzept 1.4). Formelle Bestätigung beider Gründer nach 17.2 steht noch aus (siehe OFFENE-FRAGEN).
2. **Composer-Whitelist abschließend** (PROJECT.md §1.1): dompdf, chillerlan/php-qrcode, picqer/php-barcode-generator, phpmailer, phpunit (dev), später genau ein Zahlungs-SDK.
3. **Erster Owner nur per CLI** (`cli/user_create.php`), kein Web-Bootstrap.
4. **MFA (TOTP)** als Eigenimplementierung, verpflichtend für owner/admin/finance ab erstem Nicht-Lokal-Deployment (M7).
5. **Malware-Scan-Kompensation** auf Shared Hosting: Typ-Whitelist + finfo, keine Skriptausführung, Attachment-only-Downloads, gerasterte Vorschauen (PROJECT.md §8).
6. **Pfad B ab Go-live** mit Vollzahlung im Checkout; **Rechnung automatisch bei Zahlungseingang**, nicht beim Bestellklick (Konzept 20.10).
7. **Zwei Produkttypen** (standard/configurable), mischbar im selben Bestellentwurf; Standardartikel ohne Proof-Zyklus (Konzept 20.11).
8. **Kein separater Warenkorb** – die Konfiguration ist der Bestellentwurf.
9. **Erstattungen** vorerst manuell (Anbieter-Backend + Gutschrift), kein automatischer Refund-Flow.
10. **„Ab Lager“-Standardartikel** bewusst verschoben (erst mit Lagermodul-Ausbau, Konzept 20.11 Punkt 4).

## 2026-08-14 – M0-Start (Branch m0-fundament)

11. **`.gitignore`-Korrektur:** Der ursprüngliche Eintrag `*.sql` hätte `migrations/*.sql` vom Commit ausgeschlossen. Gemäß PROJECT.md §2 (dort `*.sql.gz`) auf `*.sql.gz` geändert und `public_html/assets/img/uploads-cache/` ergänzt. Migrationen gehören versioniert ins Repo.
12. **PHP-Laufzeit lokal 8.2.12** (XAMPP) statt 8.3. PROJECT.md §3 erlaubt 8.2 ausdrücklich als Fallback („keine Syntax > 8.2 ohne Rückfrage"). Es wird keine PHP-8.3-only-Syntax verwendet; `composer.json` fordert `php: >=8.2`.
13. **Composer 2.10.2** über den offiziellen getcomposer.org-Installer installiert (Signatur SHA-384 gegen `composer.github.io/installer.sig` verifiziert), da das winget-Paket `Composer.Composer` nicht mehr im Katalog ist. Ablage `C:\xampp\php\composer.phar` + `composer.bat`, `C:\xampp\php` im User-PATH.

## 2026-08-14 – Finanzmodul (Branch feature/finanzen, bewusste Abweichung)

14. **Eigenständiges Finanzmodul außerhalb der Meilenstein-Reihenfolge** – auf Wunsch des Owners vorgezogen (Roadmap sähe Finanzen in M6/M8). Manuelle Erfassung, unabhängig von der Auftrags-Pipeline; späterer Abgleich mit M6 (Ausgaben §5.6) und M8 (Bankabgleich/Dashboard) bleibt möglich. Begründung: operative Finanzübersicht (Einnahmen/Ausgaben/Gewinn, Kapitaleinlagen und Gewinnanteil je Gründer) wird schon vor dem Pipeline-Ausbau gebraucht.
15. **Migrationsnummer 050** für `050_finance.sql`. Die Nummern **002–006 bleiben reserviert** für die geplante Roadmap (Katalog/Preis, Config/Quote, Order/Proof/Invoice, Operations, Shop-Checkout). Out-of-band-Module verwenden den 050er-Block, damit die geplante Sequenz unangetastet bleibt. Die Finanztabellen sind unabhängig (nur FK auf `users`), daher keine Reihenfolgeabhängigkeit.
16. **Neue Capability `tpb_manage_finance` => [owner, finance]** für schreibende Finanzaktionen; Lesen über die bestehende `tpb_view_costs` (owner/admin/finance). Ergänzt die Matrix aus §4 (dort als „Auszug, vollständig implementieren" gekennzeichnet).
17. **Begriffsdefinition (mit Owner geklärt):** „Investitionen pro Mitarbeiter" = **Kapitaleinlagen der Gründer** (`capital_contributions`); „Verdienste" = **Gewinnanteil je Gründer** (berechnet aus Gewinn × `profit_share_bps`, keine eigene Auszahlungstabelle). Gewinn = Einnahmen − Ausgaben; Einlagen zählen als Eigenkapital, nicht als Einnahme.
18. **Diagramme ohne Bibliothek/CDN** (§14.9): Server rendert einen JSON-Datenblock (`<script type="application/json">`, nicht ausführbar), ein eigenes ES-Modul `assets/js/finance-charts.js` erzeugt daraus Inline-SVG. CSP `default-src 'self'` bleibt unangetastet (keine Inline-Skripte).

## 2026-08-14 – M2-Grundlage (Live-Preis)

19. **Namespace `Http/Api` statt `Http/Public`**: PHP erlaubt `public` (case-insensitiv, also auch `Public`) nicht als Namensraum-Segment. Der in PROJECT.md §2 skizzierte Ordner `Http/Public/` ist als PSR-4-Namespace ungültig. Öffentliche/API-Controller liegen daher unter `app/Http/Api/` (`Tpb\Http\Api`); die späteren öffentlichen Seiten (Konfigurator, Statuslink) kommen unter `app/Http/Site/` (`Tpb\Http\Site`).
20. **`POST /api/price`** ist der serverseitige Live-Preis (§6): akzeptiert eine Konfiguration mit externen Schlüsseln (Produkt-`public_id`, Varianten-SKU, Technik-Code), rechnet gegen das aktuell veröffentlichte Preisbuch/die Kostenversion und gibt den Breakdown inkl. `calc_hash` zurück. **Kein Client-Betrag.** Persistenz in `price_calculations` folgt mit Migration 003 (Konfigurations-Tabellen).

## 2026-08-15 – Lagerbestand eingeplant (Verfeinerung von #10)

21. **Bestands-/Verfügbarkeitsanzeige im Shop – Umsetzung in M6b** (Owner-Entscheidung 2026-08-15; hebt die pauschale Verschiebung aus #10 gezielt für diese Anzeige auf). Parameter:
    - **Bestand = Rohlinge je Variante** (Print-on-Demand; konfigurierbare Produkte werden on-demand bedruckt, Bestand wird auf Rohling-/Variantenebene geführt).
    - **Reserve je Variante einstellbar**, Default **3** (Sicherheitspuffer, wird nie mitverkauft).
    - **Angezeigte Verfügbarkeit = max(0, Bestand − Reserve)**; im Shop kein Verkauf, wenn Verfügbarkeit ≤ 0.
    - **Umsetzung erst mit dem Shop (M6b):** eigene Migration (Bestands-/Reservefeld an `product_variants` oder eigene Bestandstabelle mit Bewegungen), Bestandsführung (Reservierung im Checkout, Abbuchung bei bezahlter Bestellung), Admin-Pflege, Audit auf jede Bestandsänderung. **Kein Code in M1/M2.**

## 2026-08-16 – M3-Start (Branch m1-katalog-preis, Fortsetzung)

22. **Zwei Kaufwege bestätigt (Owner 2026-08-16), präzisiert #6/#7:**
    - **Einzelbestellung** (privat, kleine Menge) = **Pfad B**: Der Kunde sieht das Ergebnis sofort im Konfigurator und **zahlt sofort** (Vollzahlung im Checkout); danach wird gedruckt. Umsetzung im **Shop-Checkout (M6b)** – blockiert auf die Zahlungsanbieter-Entscheidung (OFFENE-FRAGEN „Vor M6b“, Onboarding erst nach Gründung).
    - **Angebotsanfrage** = **Pfad A**, ausschließlich für **Clubs / größere Bestellungen**: Kontaktanfrage → Admin erstellt Angebot (Snapshot + `Q-`Nummer + PDF) → Versand → Kundenannahme per Token → Order. Das ist der Inhalt von **M3**.
    - Konsequenz für M2/M3: Die Konfigurator-Seitenleiste bietet in M3 **„Angebot anfragen“** als sichtbaren Abschluss (Pfad A). Der Sofort-Kauf-Button („zahlungspflichtig bestellen“, Pfad B) erscheint erst in M6b, wenn der Zahlungsanbieter steht. Bis dahin ist die Anfrage der einzige verbindliche Abschlussweg.
23. **dompdf 3.1.6** installiert (Whitelist §1.1 / #2). `composer audit`: keine Advisories. Härtung nach §9: `isRemoteEnabled=false`, `chroot` auf `Views/pdf` + Temp; alle dynamischen Werte über `e()` in den PDF-Views; Bilder nur aus lokalem privaten Speicher.
24. **Migration 004 vollständig** nach §5.5 angelegt (orders, order_items, order_item_units, order_terms_acceptance, artwork_versions, proofs, proof_approvals, production_jobs, production_events, invoices, invoice_lines, payments, payment_allocations, deposit_requests, print_jobs). M3 nutzt nur den Order-/Terms-/Deposit-Teil; die übrigen Tabellen bleiben bis M4–M6 leer, werden aber jetzt erstellt, damit 004 nach dem Merge unveränderlich bleibt (§14.5). `quotes.amends_order_id` bleibt ohne FK (Reihenfolge, #s.o.); der Index genügt.
35. **M9 Nachträge & Kapazität – 2026-08-16.** **Nachtragsangebot** (`quotes.amends_order_id`): `QuoteService::createFromConfiguration(..., $amendsOrderId)` bindet das Angebot an eine bestehende Order (Kunde von dort), Preis nach **aktuellem** Preisbuch. Annahme (§7): **keine neue Order** – `OrderRepo::appendFromSnapshot` hängt neue order_items(+Units) an, `orders.total_cents` additiv; **idempotent** (FOR UPDATE + Statusprüfung; zweite Annahme liefert dieselbe Order). Bestehende Snapshots (Ursprungsangebot/Rechnungen) bleiben binär unverändert. Konfigurierter Nachtrag bei gesperrtem Artwork ⇒ **neuer Proof-Zyklus** (neue Statesregel `artwork LOCKED → MISSING`, nur für Nachträge). **Kapazität:** `planned_min` je Job aus der aktiven Kostenversion (SETUP_MIN + (UNIT_MIN+MACHINE_MIN)×Menge); `CapacityReport` aggregiert je Fälligkeitswoche gegen `business_settings.capacity.week_minutes` (Ampel grün/gelb/rot, **kein Auto-Block**); Admin `/admin/kapazitaet` + Termin je Job. **Vertretungstest** (zweiter Gründer nach RUNBOOK) ist ein **manueller** Abnahmeschritt – nicht automatisierbar, in RUNBOOK.md verankert.

34. **M8 Zahlungsabgleich & Forderungen – 2026-08-16 (Migration 005 genutzt).** Bankimport (`Domain/Bank`): **CSV** (dokumentiertes Standardformat) und **CAMT.053** (ISO 20022, Namespaces ignoriert). **Doppelter Duplikatschutz:** `file_sha256` (ganze Datei ⇒ identische Datei = 0 neue Zeilen) UND `dedupe_hash` je Zeile = sha256(Konto+Buchungsdatum+Betrag+Verwendungszweck+Zeilennr.). **Matching** (`Matcher`): Rechnungs-/Auftragsnummer im Verwendungszweck/EndToEndId ⇒ hoch; Betrag exakt + Namensähnlichkeit ⇒ niedrig. **Nie automatisch buchen** – Bestätigung durch finance/owner erzeugt in EINER Transaktion Payment + Allocation + abgeleitete Zahlungsachse (Eingang) bzw. Ausgabe (Ausgang) und setzt `match_status=confirmed`. **Mahnwesen** (`DunningService`, `cli/reminders.php`): Zahlungserinnerung Stufe 1 für überfällige Rechnungen + Wiedervorlage ablaufender Angebote, je Stufe genau einmal (`reminders_sent` UNIQUE). **Dashboard-„Heute"-Liste** (`TodayList`): ablaufende Angebote, wartende Proofs, überfällige Rechnungen, offene Bankzeilen, blockierte Jobs. Recht `tpb_manage_finance`. Konkretes Hausbank-Spaltenmapping + weitere Mahnstufen/Textbausteine bleiben offen (OFFENE-FRAGEN).

33. **M7 Härtung – Teilumsetzung 2026-08-16.** **TOTP-MFA** als Eigenimplementierung (`Domain/Auth/Totp`, RFC 6238, getestet gegen die RFC-Vektoren) + `MfaService` (Aktivierung mit Code-Bestätigung, 8 einmalige Backup-Codes nur als SHA-256, Deaktivierung). Login zweistufig (Passwort → MFA-Code/Backup); für owner/admin/finance **ab Nicht-Lokal-Deployment verpflichtend** (lokal optional, Einrichtung erzwingt Redirect auf `/admin/mfa`). QR über `/admin/mfa/qr` als PNG-**Endpunkt** (kein `data:`-URI ⇒ CSP `default-src 'self'` bleibt streng). **Betrieb:** `Domain/Ops/HealthCheck` (DB/Backup/Outbox/Disk/Quarantäne) + öffentlicher **`/health`** (nur Status, keine sensiblen Daten, 200/503), `cli/healthcheck.php` (Mail nur bei Problemen + Montags-Summary), `cli/retention.php` (fällige Assets je `retention_class` löschen, referenzierte via FK übersprungen, Standard-Trockenlauf, `--apply`). Doku-Grundgerüst `docs/RUNBOOK.md` + `docs/DEPLOY.md`. **Noch offen für M7-Abschluss (beim Deployment):** sudo-Modus (Re-Auth für kritische Aktionen), Checkout-Rechtsprüfung gegen Kapitel 12, Backup/Restore-Test dokumentiert, Seeds als End-to-End-Durchlauf, konkrete Hostinger-Schritte.

32. **Versandkosten-Logik (Owner 2026-08-16).** `Domain/Shipping/ShippingRates` bestimmt je Lieferland die Zone: **national (LU) = Post Luxembourg**, **international = DHL**. Kosten sind **im Preis einkalkuliert**: bei Pfad-B-Checkout wird der Versand nach Zone berechnet und zur Bestellsumme addiert (Order-Total = Produkt + Versand = Zahlbetrag = Rechnungssumme; Versand erscheint als Rechnungszeile über die Sendung). **National gratis ab Schwellenwert** (Default 50 €). Beträge/Carrier/Schwelle aus `business_settings` (`shipping.*`, Platzhalter, DB-/admin-pflegbar) – nie hart codiert; **echte Zonen-Beträge noch offen** (OFFENE-FRAGEN). Die Zone setzt zugleich `shipment.method=carrier_standard` + `carrier`; echtes Carrier-Label/Tracking folgt mit API-Zugang (bis dahin Hausetikett).

31. **Lagermodul (Owner 2026-08-16), Migration 052 (Out-of-band-Block).** Bestand je Variante (`product_variants.stock_qty/reserve_qty/reorder_threshold`) + `stock_movements` (Audit jeder physischen Änderung), `stock_reservations` (Checkout-Holds), `stock_alerts` (Meldebestand-Alarme, dedup je Stufe). **Verfügbarkeit = max(0, stock_qty − reserve_qty − aktive Reservierungen)**; Reserve wird nie angezeigt/verkauft. **Ablauf:** Shop/Konfigurator zeigt „noch X" + Warnung bei geringem Bestand; **Checkout reserviert** (SELECT … FOR UPDATE, kein Oversell); **Abbuchung erst bei bezahlter Bestellung** (Webhook → `consumeForOrder`, `stock_movements.reason=sale`); **Freigabe** bei Abbruch/Ablauf (`cli/expire.php`, Checkout-Cancel). **Alarme** (Outbox an Owner) bei Unterschreiten Meldebestand (Default 5), dann kritisch (≤2), dann leer (≤0) – einmalig je Stufe, Rücksetzung bei Wiederauffüllung. **Admin `/admin/lager`:** Sofort-Übersicht, Wareneingang/Korrektur/Reserve/Meldebestand je Variante + „für alle setzen". Pflege **manuell**; automatischer Wareneingang (Lieferschein) folgt. Recht `tpb_manage_pricing`.

30. **M6b Shop-Checkout mit Test-Adapter (Owner 2026-08-16).** Migration 006 (`payment_intents`, `payment_webhook_events`, §5.7). Pfad B: Checkout auf der Konfiguration → Order **PENDING_PAYMENT** (Preis IMMER serverseitig neu berechnet, Client-Betrag ignoriert) + `payment_intents` → gehostete Bezahlseite. **Gateway-Abstraktion** `Domain/Payment/Gateway` (`PaymentGateway` + `TestGateway` + `GatewayFactory`); `PAYMENT_PROVIDER=test`, HMAC-Signatur über `PAYMENT_WEBHOOK_SECRET`. Test-Adapter simuliert die Anbieterseite lokal (`/pay/{intent}`) und löst denselben **signierten Webhook** aus, den ein echter Anbieter server-to-server schickt. **Webhook (§5.7):** Signatur zuerst prüfen → Rohevent als Beleg persistieren → Betrag/Währung gegen Order abgleichen (Mismatch ⇒ Alarm-Audit, keine Buchung) → **Idempotenz doppelt** (`UNIQUE(provider,event_ref)` UND `idempotency_keys shop_paid_<order>`) → Rechnung ausstellen (Issue-Flow) + Zahlung buchen/zuordnen (→ PAID) + Order **CONFIRMED** + Outbox-Mails. `cli/expire.php` setzt abgelaufene PENDING_PAYMENT-Orders (Order-Achse aus `status_events`, keine Cache-Spalte) und abgelaufene Angebote auf EXPIRED. **Snapshot-Builder** in `OrderSnapshot` extrahiert (geteilt Angebot/Checkout); `OrderRepo::createFromSnapshot` mit variablem Startzustand (CONFIRMED/PENDING_PAYMENT). **Echter Anbieter + KYC:** erst nach Gründung (Go-live-Blocker, OFFENE-FRAGEN). **Lager/Bestand (DECISIONS #21) noch NICHT umgesetzt** – offene Sub-Fragen (harte Sperre vs. Warnung, Wareneingang) unbeantwortet; Shop bucht derzeit ohne Bestandsführung.

29. **M6 Rechnung – Steuer/Verkäufer als Platzhalter (DECISIONS-Muster wie #25).** Migration 005 (`expenses` genutzt; bank_imports/bank_lines/reminders_sent für M8 vorab angelegt, §14.5). Rechnungsnummer `2026-000001` (eigene Sequenz `invoice`, ohne Präfix, §9.3) via `NumberSequence::nextInvoice`. Issue-Flow §11.2: Nummer ziehen → Seller/Customer/Tax/Lines einfrieren → **kanonisches JSON und PDF aus DEMSELBEN Snapshot** → beide als Assets (`invoice_10y`) + SHA-256 → ISSUED (unumkehrbar). **Steuer:** aktives Regime aus `tax_regime_versions` (Seed `FRANCHISE_57BIS` = Franchise/Kleinunternehmer Art. 57bis → **0 % USt**, Legende als PLATZHALTER; echter Volltext + Verkäufer-Snapshot blockieren Go-live, OFFENE-FRAGEN). Zahlungsachse **abgeleitet** (`PaymentService::deriveForOrder`, nie manuell): Forderung (ausgestellte Belege) vs. Eingang → NOT_DUE/UNPAID/PARTIALLY_PAID/PAID; Umsatz ≠ Zahlungseingang bleiben getrennt. Gutschrift = eigener Beleg `credit_note` mit `credited_invoice_id`, negierten Positionen, eigener Nummer; Original → FULLY_CREDITED. **USt-Regime bei tatsächlicher USt-Pflicht später erweitern** (derzeit nur Franchise/0 %).

28. **Versand vorgezogen (Owner 2026-08-16), Migration 051 (Out-of-band-Block, DECISIONS #15).** Es wird versendet: **Premium = Eigenlieferung** (Hausetikett, kein Carrier), **Standard = günstigster externer Anbieter** (Carrier pro Sendung variabel). Jetzt gebaut: `shipments`-Tabelle (eine Sendung je Auftrag), Versandart/Lieferadresse/Kosten, Fulfillment-Achse (§7: UNFULFILLED→PACKING→READY_FOR_PICKUP→COLLECTED **oder** READY_TO_SHIP→SHIPPED→DELIVERED) über `orders.cur_fulfillment` + `status_events`, **Hausetikett** (Empfänger + Auftrags-Barcode/QR, 100×150 mm) über `print_jobs` (Neudruck nur mit Grund). Carrier-Anbindung über generische `CarrierAdapter`-Schnittstelle + Registry („günstigster Anbieter"); vorerst `HouseCarrier` (Eigenlieferung/manuelle Kosten, kein Tracking). Echte Carrier-Label/Tracking + stiller Autodruck bleiben extern blockiert (OFFENE-FRAGEN „Vor M6b"). Migrationsnummer **051**, weil 005/006 für Operations/Shop reserviert sind (#15).

26. **QR/Barcode-Libs (M5, Whitelist §1.1):** `chillerlan/php-qrcode` 6.0.1 + `picqer/php-barcode-generator` 3.2.4 installiert. `composer audit` sauber. QR/Code128 werden als **PNG-Data-URIs** erzeugt (lokal, kein Netzzugriff) und ins Etikett-PDF eingebettet (dompdf `isRemoteEnabled=false` bleibt gültig). Jobetikett 62×100 mm über `PdfService::renderLabel` (Papiermaß in Punkten). Zusätzlich GD-Testrender (`cli/label_test.php`) in 203/300 dpi als PNG (M5-DoD, Sichtprüfung).
27. **Produktions-Statusachse je Job:** `status_events` mit `aggregate_type='production_job'`, `axis='production'`; `production_jobs.status` ist die Job-Wahrheit, `orders.cur_production` ein Rollup-Cache. Freigabe-Gate (§7.3) serverseitig in `ProductionService::release`. Mengen-/Ausschusserfassung idempotent über `production_events.idem_key` (UNIQUE).

25. **Rechtstexte als klar markierte Platzhalter** (`legal_document_versions`, Status `published`, Version `v0-PLATZHALTER`): AGB, Datenschutz, Widerruf/Erlöschen bei personalisierter Ware, Datei-Erklärung. Inhalt ist **kein Rechtstext**, sondern ein sichtbarer Hinweis „Platzhalter – juristische Fassung ausstehend“. So bleibt der Consent-/Snapshot-Mechanismus testbar, ohne Fachwerte zu erfinden (§14.4). Ersetzen, sobald die beauftragten Texte vorliegen (OFFENE-FRAGEN „Vor M3“).
